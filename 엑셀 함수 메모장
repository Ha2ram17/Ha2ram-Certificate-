함수의 기본 특성
함수는 입력이 끝나고 엔터를 눌러 수정이 끝나면 작동한다.
함수는 여러개를 사용할 수 있고, 함수안에 함수를 넣어 사용할 수도 있다.

CHOOSE : 범위 안에서 조건에 맞는 셀을 찾는 함수
RANK.EQ : 수 목록 내에서 크기 순위를 구하는 함수. 내림차순으로 할지, 오름차순으로 할 지 결정할 수 있다.
AVERAGE : 범위 내에서 수들로 평균을 구하는 함수
VLOOKUP : 배열의 첫 열에서 값을 검색하고, 지정한 열의 같은 행에서 검색한 데이터를 반환해줌. 디폴트값으로 오름차순으로 정렬됨.
LOWER : 범위 내의 문자를 소문자로 반환하여 출력하는 함수
UPPER : 범위 내의 문자를 대문자로 반환하여 출력하는 함수
FIND : 검색하는 함수이지만, 대문자와 소문자를 구분하여 정확하게 검색하고 반환한다.
SEARCH : 검색하는 함수이지만, 대문자와 소문자 구분없이 검색하고 반환한다. 
REPT : 설정힌 범위를 반복하여 반환하는 함수이다. 
LEN : 문자열의 길이를 세어주고 반환하는 함수이다.
LEFT : 설정한 범위를 추출하여 반환하는 함수이다. 
MID : 슬라이싱과 동일한 역할이다. 범위만큼 잘라내고 반환한다. 
RIGHT : 문자열의 오른쪽부터 지정한 범위만큼 잘라내고 반환한다. 
YEAR : 날짜에서 연도만 추출하여 반환하는 함수
MONTH : 날짜에서 월만 추출하여 반환하는 함수
DAY : 날짜에서 일만 추출하여 반환하는 함수
WEEKDAY : 날짜에 해당하는 요일번호를 반환함. (1=일요일 ~ 7=토요일)
DAYS : 마지막 날짜와 시작 날짜를 정하고, 마지막 날짜에서 시작 날짜를 뺀 일 수를 계산하고 반환하는 함수이다
DATE : 년, 월, 일에 대한 날짜의 일련번호를 반환한다.
TODAY : 컴퓨터 기준으로, 현재 날짜를 반환하는 함수이다.
EDATE : 시작날짜에서 월수를 더한 날짜를 반환하는 함수이다. (월수 - 양수 : 이후 날짜를 대상으로 값을 구한다. 음수 : 이전 날짜를 대상으로 값을 구한다.)
EOMONTH : 지정한 날짜를 기준으로 몇 개월 이전 또는 이후 달의 마지막 날짜를 반환하는 함수이다. ((월수 - 양수 : 이후 날짜를 대상으로 값을 구한다. 음수 : 이전 날짜를 대상으로 값을 구한다.)K
WORKDAY : 시작날짜에 주말과 휴일날짜를 제외한 후 일수만큼 지난 날짜를 반환하는 함수이다.
HOUR : 시간에서 시만 추출하여 반환하는 함수이다.
MINUTE : 시간에서 분만 추출하여 반환하는 함수이다.
SECOND : 시간에서 초만 추출하여 반환하는 함수이다.
TIME : 시,분,초에 대한 시간의 일련번호를 반환한다.
NOW : 현재 날짜와 시간을 반환한다.
MAX : 범위중 가장 큰 값을 반환하는 함수이다.
MIN : 범위중 가장 작은 값을 반환하는 함수이다.

지속적으로 업데이트하는 문서입니다.
